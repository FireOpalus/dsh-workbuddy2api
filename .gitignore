# 依赖
node_modules/

# 构建产物 lib/ 是 **刻意提交** 的，不要加回这里。
#
# 原因：pnpm 对 git 依赖有一道门 —— 只有当 package.json 带
# prepare/prepublish/prepack/publish 之一、**并且** 仓库里找不到 main 指向的
# 文件（lib/index.js）时，才判定「这个包需要跑构建脚本」，进而要求用户把它
# 加进 pnpm-workspace.yaml 的 allowBuilds，否则直接
# ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED 拒绝安装。
# lib/ 一旦被 ignore，克隆出来的仓库就永远缺 main 文件，于是每个用
# `dsh plugin add git+https://…` 的人都会撞上这道门。
# 提交 lib/ 之后 main 存在，pnpm 判定「不需要构建」，开箱即装；
# prepack 仍保留，所以 `npm pack` / `npm publish` 照旧会先构建。
# 同机的 dsh-laa 也是把 lib/ 提交进仓库的（它连 prepack 都没有）。

# 打包与产物校验的中间目录。
dist-pack/

# 隔离测试环境：只属于本仓库的 DSH home，由 testenv/setup.mjs 创建。
# 它含有从真实 home 复制来的凭据与运行期会话数据，绝不能提交。
# dsh-home-* 一并覆盖「装/升级验证」用的第二个 home（如 dsh-home-update）。
testenv/dsh-home/
testenv/dsh-home-*/
testenv/dsh-home-pack/
testenv/pack-extract/

# 依赖安装缓存与早期验证用的临时目录。
.npm-cache/
# pnpm 在本仓库里跑过一次就会留下这个 store（含索引库），不是源码。
.pnpm-store/
.scratch-net/

# 调研参照物的本地副本（第三方源码，不随本仓库分发）。
.reference/

# 编辑器与系统噪音
.DS_Store
Thumbs.db
*.log