# Register the WorkBuddy account-pool provider without changing the profile's
# default model. The plugin reuses the WorkBuddy desktop app's sign-ins; no
# separate OAuth flow is started.
- insert:
    - id: dsh-workbuddy2api
      name: dsh-workbuddy2api
