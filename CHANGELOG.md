# 更新日志

## [0.6.8] - 2026-09-25

### 修复 DSH 0.1.7-rc.2 设置页空白

- 实际复现的原因是宿主删除了 `IconChevronDownOutline14` 导出，插件仍将其作为 React
  组件渲染，触发 React #130。宿主隔离失败的槽位内容后，侧栏入口仍在，正文为空。
  0.6.7 调整默认展开状态未解决这个错误。
- 两处折叠箭头改用插件内的 SVG，兼容新旧宿主的图标 API。
- 独立页面使用 `section` 和静态标题，避免点击标题又将整个页面折叠；旧行内模式仍可折叠。
- 增加真实 React 渲染回归测试，覆盖缺失旧图标导出时的独立页面与行内模式。
- 修正 README 中「设置页暂未迁移」的过时说明；宿主兼容性声明补上 `<0.2.0` 上限。

兼容性：保留 0.1.5-rc.2 支持；在 0.1.7-rc.2 隔离网页验证设置页。未声明支持 0.2.x。

## [0.6.7] - 2026-09-25

### 修复：设置页出现但**里面是空的**

侧栏出现「WorkBuddy 账号池」了，点进去却什么都没有。原因很直接：

**这张卡片是照「列表里可折叠的一行」写的**：

```jsx
<li className="dsm-plugin-card">
  <button onClick={() => setOpen(!open)}>…</button>          // 折叠头
  <div className="dsm-plugin-card-body" hidden={!open}>      // open 初始为 false
```

`open` 初始是 `false`，内容就一直是 `hidden`。在旧的「插件配置」里这是对的 ——
它就是个折叠行，用户点一下展开；**但一个页面没有人去点它**，于是渲染出折叠头和一片空白。

现在按注册方式区分两种形态：

| 注册到 | 形态 | 初始状态 |
|---|---|---|
| `settings.section`（独立页面） | 页面 | **展开** |
| `settings.plugin.item`（行内条目，旧线） | 折叠行 | 折叠（保持原行为） |

### 兼容性声明（本版起）

`package.json` 的 `dsh.engines.dsh` 现在声明支持范围（写法对齐 `@linxin666` 的插件）：

```json
"dsh": { "engines": { "dsh": ">=0.1.5-rc.2 || >=0.1.7-rc.2" } }
```

为什么不是简单写 `>=0.1.5-rc.2`：按 semver 规则，**预发布版本只有在范围里存在
「同一 major.minor.patch 且带预发布」的比较器时才匹配**，所以 0.1.7 的元组必须显式列出 ——
否则声明会变成"不支持 0.1.7-rc.2"。守护测试同时锁住这两个元组。

## [0.6.6] - 2026-09-25

### 修复：卡片挂到了错误的槽位（`settings.plugin.item`）

0.6.5 之后控制台终于出现：

```
[dsh-workbuddy2api] settings card mounted into settings.plugin.item
```

**注册成功了，但挂错了地方** —— `settings.plugin.item` 是"别人页面里的一行"，
而你在侧栏找它，自然找不到。

原因是我用 `specDynamic('settings.section')` 当判据。它在这台宿主上答"不存在"，
**可是往这个槽位注册明明是可以的** —— 插件市场 / 远程访问 / 归档会话三个页面都这么做，
而且它们就在你的侧栏里。**我拿一个不可靠的探针，否决了一个可行的事实。**

现在改成**只信槽位自己的声明事件**：`settings.section` 一声明就立刻挂上去；
`settings.plugin.item` 则**先等 500ms** —— 因为 0.1.7-rc.2 两个槽位都有，
不等的话卡片又会被塞进别人的列表里而且再也不会搬家。

### 这一轮的教训

前五次失败，每一次我都"找到了一个说得通的原因"，然后据此改代码。
问题不在推理能力，在于**我把探针的结果当成了事实**：`specDynamic` 说没有，
我就信了，没去问"那三个能用的插件是怎么做的"。

## [0.6.5] - 2026-09-25

### 修复：真正的根因 —— 读一个没注入的服务会**抛异常**，不是返回 undefined

浏览器控制台终于给出了确切答案（我上一条请求的那行输出）：

```
[dsh-workbuddy2api] client card failed to load (host provider unaffected):
Error: cannot get property "settingsScope" without inject
    at new apply (client.js:2312:1)
```

就是这个。cordis 的 context 代理在**读取未注入的服务属性时抛异常**，而我写的是：

```js
const settingsScope = ctx.settingsScope === undefined   // ← 这里就抛了
  ? createRouteSettingsScope(...)
  : ctx.settingsScope.bind(...)
```

0.1.7-rc.2 移除了 `settingsScope`，我把「不再必需」这件事做对了（否则条目 pending），
但**读取它的那一行**仍然在 —— 于是 `apply` 在**任何注册发生之前**就抛了，
被我的 try/catch 吞掉，表现为「没有卡片、也没有任何输出」。

这也解释了为什么 0.6.3 加的 `mounted` / `could not mount` 两行**一个字都没出现**：
异常发生在它们之前。我一直在找"注册为什么没生效"，而实际上**注册代码根本没被执行到**。

**修法**：改用 `ctx.get('settingsScope')` —— 这是**会回答"不存在"而不是抛异常**的那个读法，
并且整体再包一层 try/catch（"这台宿主没有设置服务"在这里是正常状态，不是错误）。

### 代价与反省

这个 bug 一行就修完了，代价是**四个版本**（0.6.0 → 0.6.4）和一个错误的清单改动方向。
根子上是我在**看不到浏览器时用推理代替验证**，而且连续几版都只盯着"注册逻辑"，
没有先去确认"我的代码到底执行到了哪一步"。0.6.3 的自报输出是对的思路 —— 它本来一眼就能
定位，我却是在你贴出控制台之后才真正用上它。

已加守护测试（249 条）：源码中不得出现 `ctx.settingsScope ===` / `ctx.settingsScope.bind`，
必须通过 `get.call(ctx, 'settingsScope')` 读取。

## [0.6.4] - 2026-09-25

### 修复：设置页仍不出现 —— 清单里声明了两个「不是客户端模块」的依赖

按你的建议去**逐个比对同机上能正常显示页面的插件**（插件市场 / 远程访问 / 归档会话 / 自动审批），
得到一条我之前完全没看的线索 —— 它们 `package.json` 的 `dsh.client.inject` **都没有**
这两项：

| 包 | 能用的插件里有吗 | 我原来的清单 |
|---|---|---|
| `@deepseek-ai/dsh-client-ui-settings-plugins` | **没有一个**有 | 有 |
| `@deepseek-ai/dsh-client-ui-primitives` | **没有一个**有 | 有 |

而**同一台机器上的 `dsh-connect-workbuddy`（与本插件同一份 inject 清单）侧栏里也没有卡片** ——
两个用同一份陈旧的依赖清单的插件，在同一条 DSH 线上**一起失效**。这比我在自己代码里反复找更说明问题。

反证也在：能用的那几个清单各不相同，但**都**排除了这两个包。插件市场源码里甚至留了一句注释：

> a value import from `dsh-client-ui-settings-plugins` would fail the client bundle-purity gate anyway

这两个是**库/宿主侧**的包，不是可加载的客户端插件模块。把它们声明成客户端依赖，
条目就可能一直等一个永远不会出现的模块 —— 而且是**静默**的（横幅只报「等服务」，
不报「等模块」），所以三版都没留下任何线索。

现在 `dsh.client.inject` 只保留三个确实存在、且被能用的插件证明可加载的项：
`dsh-client-locale`、`dsh-client-ui-settings`、`dsh-client-ui-slots`。

> 这条修法**尚未在你机器上验证**（我改的是包清单，必须重装才生效）。
> 但如果侧栏出现了，就说明根因在这里；如果没有，我下一步会直接读你浏览器控制台的那两行。

## [0.6.3] - 2026-09-22

### 修复：设置页仍未出现 —— 改为「在槽位被声明时注册」

0.6.2 修了同步检查，但页面还是没出现，而且**依旧没有任何报错**（控制台里连我包在
`apply` 外面的 try/catch 日志都没有）。已经查明的部分：

| 已确认 | 结论 |
|---|---|
| 浏览器确实加载了我的客户端包 | 启动清单里有 `dsh-workbuddy2api/client.js` |
| 没有 `1 entry did not activate` 横幅 | 条目已激活，`apply` 跑到了 |
| 产物内容正确 | `card.pageTitle`、两个槽位名、`specDynamic` 都在；只 require `react` / primitives / jsx-runtime |

所以问题只剩一个：**注册没有落到任何会渲染的地方**，而且**静默失败**。

0.6.2 依赖 `slots.inject` 去「等待」槽位声明 —— 我没有验证过它是否真的会等，
现在看它显然没有按我以为的方式工作。本版改成**显式订阅声明**：

```
槽位已声明 -> 立刻注册
否则       -> subscribeDeclaration(key, ...)，声明出现时注册
再否则     -> inject（旧线兜底）
5 秒后仍未挂载 -> 明确 warn 出两个槽位的实际状态
```

### 这一版的重点其实是「不再静默」

前三次失败都有一个共同点：**什么都不做，也不说**。所以本版加了两条可见输出：

- 挂载成功：`[dsh-workbuddy2api] settings card mounted into <槽位名>`
- 挂载失败：`[dsh-workbuddy2api] settings card could not mount: ... { section, pluginItem }`

我看不到你的浏览器，所以需要这两行来把猜测变成事实。

## [0.6.2] - 2026-09-22

### 修复：设置页在 0.1.7-rc.2 上根本没出现

现象很有迷惑性：**没有报错、没有横幅、所有测试都过，但侧栏里就是没有「WorkBuddy 账号池」**。

根因是我在 0.6.0 里做的一个错误假设 —— **用同步的槽位存在性检查来决定注册到哪个槽位**：

```js
if (slots.specDynamic('settings.section') !== undefined) {   // ← 此刻还没被声明
  ctx.slots.inject('settings.section', ...)                   // 走不到
} else {
  ctx.slots.inject('settings.plugin.item', ...)               // 落到一个不存在的槽位
}
```

`settings.section` 这个槽位是**别的浏览器插件声明**的，而在我 `apply` 的那一刻，那个声明
可能还没发生。于是检查答「不存在」→ 我注册到旧槽位 → **旧槽位在这一版也不存在 → 什么都没渲染，
且不会报任何错**。

**正确做法是不用问、直接用 `inject`**：它的职责正是**等待槽位被声明**。
现在两条 `inject` 都装上，谁先触发谁挂载卡片；若宿主同时声明两者，新的优先。

> 这条修得比较惭愧：官方插件全都用 `slots.inject` 等待声明，而它们的做法我读过 ——
> 却仍然在图省事时加了那个同步检查。已加守护测试锁死：源码里必须存在**两个**
> `() => mount()` 装配点，退回旧的早退写法会立刻失败。

## [0.6.1] - 2026-09-22

### 修复：0.6.0 的设置写入被宿主拒绝

0.6.0 让卡片改走自有配置路由，但 POST 被宿主拒绝：

```
500 Plugin entry "dsh-workbuddy2api" has no volatile fields
```

原因是宿主设置服务的 `update()` **只接受触及 `volatile` 字段的补丁** ——
`volatile`（`.volatile()` 标记）表示「这个字段可以在运行期改，且不需要重新 apply 插件」。
本插件的 Config 里一个 volatile 字段都没有，于是整条写入路径不可用（GET 读是好的）。

现在把卡片实际会写的两个字段标成 volatile：`tasks`（定时设置）与 `regions`（模型选择/池状态）。

**应用方式刻意保守**：若运行期的 schema 构造器没有这个修饰符，就原样返回 schema。缺一个修饰符
应当只损失设置表单，绝不能让插件加载失败 —— 这正是 0.5.1 的教训（一处 API 用错，
把全部模型一起带下去）。

### 实测（真实 DSH 0.1.7-rc.2）

```
GET  /plugins/dsh-workbuddy2api/config          -> 200  writable: true
POST /plugins/dsh-workbuddy2api/config          -> 200  (写回同一个值，验证链路)
POST 不带 field                                  -> 400  {"error":"field is required"}
```

### 一个观察（尚未完全解释，如实记录）

标记 volatile 之后，`GET` 返回的文档从 8 个顶层键变成 2 个（`tasks`、`regions`）。
卡片只读这两个键（`regions[region]` 与 `tasks`），所以功能不受影响；
其余键（`poolState`/`pool`/`lastCatalog` 等）由宿主自己持久化，卡片从不读。
但这确实是一个行为变化，成因我还在确认，不想含糊带过。

## [0.6.0] - 2026-09-22

### 设置页迁移完成：0.1.7-rc.2 上功能与旧线一致

0.5.x 解决了「加载不了」，但卡片在 0.1.7-rc.2 上只能看、不能改 —— 它的设置读写依赖
已被移除的 `settingsScope`。本版把那条路补上，**且不是重写一套设置 API**。

### 做法：保留卡片原有的三成员接口，换掉背后的实现

卡片的 18 处调用一行未改。宿主新增一个配置路由，浏览器侧用一个**接口同形**的适配器接上它：

```
宿主没有 settingsScope（0.1.7+） -> createRouteSettingsScope({ url: /plugins/dsh-workbuddy2api/config })
宿主有 settingsScope（旧线）     -> 继续用框架的 service，行为一字不变
```

三个形态是被卡片的用法**逼出来的**，不是随意选的：

- `getSnapshot()` 在**渲染期**被调用，必须是**同步**的 → 适配器缓存上次读到的文档；
- 因此变更只能靠 `subscribe()` 送达 → 每次刷新都通知订阅者；
- `set()` 在宿主上**只合并一个顶层字段**，然后**重新读取** →
  表单显示的永远是宿主真正存下的值，而不是它发出去的回声。

### 为什么是「合并一个字段」而不是「整段替换」

框架的说明里写得很清楚：线上传输**从不携带标记为 secret 的字段**。
如果按浏览器手上那份重建整段再写回，会把所有密钥字段**静默删掉**。
所以宿主只接受单字段合并（`ctx.settings.update(entryId, { [field]: value })`）。

### 路由

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/plugins/dsh-workbuddy2api/config` | 返回整段配置 + 是否可写 |
| POST | `/plugins/dsh-workbuddy2api/config` | 合并一个顶层字段（`{ field, value }`） |

宿主没有配置路由时返回 501，只读部署返回 403 —— 卡片据此进入只读态。

### 失败时的行为

**宿主不可达只损失设置表单，绝不损失条目。** 适配器不抛异常：
读失败就停在 `status: 'error'` / `writable: false`，卡片照常渲染账号池。
这条有测试锁死 —— 0.5.1 的教训正是「让条目激活失败」比「少个功能」严重得多。

### 测试

新增 `tests/route-scope.spec.ts`（8 条）覆盖：同步快照、首读前后、订阅与退订、
单字段合并 + 重新读取、宿主拒绝的错误透传、宿主不可达时降级为只读、单个订阅者抛异常不影响其他。
另在 `tests/host-compat.spec.ts` 增加 4 条客户端守护（不得要求 `settingsScope`、
必须同时支持新旧槽位、必须回落到自有路由）。**共 247 条。**

## [0.5.2] - 2026-09-22

### 修复：0.1.7-rc.2 上启动横幅 "Failed to load plugins"

0.5.1 修好了宿主半，但浏览器半仍把 `settingsScope` 列为**必需**服务。该服务在
0.1.7-rc.2 已被移除，于是 DSH 把整个条目判为未激活：

```
web boot: 1 entry did not activate
dsh-workbuddy2api: pending (waiting for service: settingsScope)
```

**必需服务缺失不是"少个卡片"，而是启动级的红色横幅** —— 这比卡片不显示严重得多，
所以 `settingsScope` 不再必需，改为可选解析（卡片本来就把每次读取都做了空值保护）。

### 新增：卡片升级为独立设置页

0.1.7-rc.2 用 `settings.section`（设置面板里的**独立页面**）取代了
`settings.plugin.item`（别人页面里的一行）。现在**按宿主实际声明的槽位注册**，
而不是假定某一个：

```
宿主声明 settings.section  -> 注册为独立页面（order 60，标题「WorkBuddy 账号池」）
否则                        -> 旧的 settings.plugin.item 行内条目
```

检测用 `slots.specDynamic('settings.section')`，所以新旧两线同一份产物都能用。

### 本版在 0.1.7-rc.2 上的实际状态

- **启动横幅消失**，条目正常激活
- **设置面板里出现独立页面「WorkBuddy 账号池」**，账号池、健康度、积分可见
- **设置类控件暂不可用**（改模型选择、改定时时间、浏览器登录加号）——
  它们依赖已被移除的 `settingsScope`，读写在下一版改由宿主路由支撑
- 模型调用、账号池轮换、冷却熔断、定时任务、启动签到**全部正常**
- 命令行 `dsh-workbuddy2api <doctor|status|pool|logout>` 正常

## [0.5.1] - 2026-09-22

### 修复（重要）：DSH 0.1.7-rc.2 上插件整个加载失败

DSH 0.1.7-rc.2 **移除了 `ctx.settings.installSection`**（全发行版 0 处引用）。本插件在
`apply` 里直接调用它、也没有兜底，于是在该版本上：

> `apply` 抛异常 → 插件整体注册失败 → **workbuddy2api 的模型在 DSH 里全部消失**

现在改为**按能力探测、两线共存**，一份产物同时支持新旧两代：

```
有 configure()  → 0.1.7+：configure({ auto: false }, ctx.fiber)，
                配置改由 describe() 按「profile 条目 id」读回
有 installSection() → 旧线：行为一字不变
```

三个要点：

- **按条目 id 读配置**：0.1.7 用 `ctx.fiber.entry.options.id`（即 `dsh-workbuddy2api`）
  作为设置键，不再接受插件自报的 `workbuddy2api`，所以两个 id 都尝试。
- **每次读、不缓存**：新线没有 `onChange` 回调（只有 `revision`），缓存会一直发旧配置。
- **`auto: false`**：本插件自带页面，不能让框架再按 schema 自动生成一个。

### 兼容声明：修掉一个"静默排除 0.1.7"的范围 bug

`peerDependencies` 原为 `>=0.1.5-rc.2 <0.2.0`。按 semver 规则，**预发布版本只有在范围里
存在「同一 major.minor.patch 且带预发布」的比较器时才匹配**，而 0.1.5 的元组管不到 0.1.7：

```
0.1.7-rc.2  satisfies  >=0.1.5-rc.2 <0.2.0                        ->  false   ← 就是这里
0.1.7-rc.2  satisfies  >=0.1.5-rc.2 <0.2.0 || >=0.1.7-rc.2 <0.2.0 ->  true
```

也就是说，**即使代码修好，包层面它仍在声明"不支持 0.1.7-rc2"**。现已为每个
`@deepseek-ai/dsh-*` 依赖补上 0.1.7 元组的比较器。

> 注意这是 semver 预发布的固有行为：**每新增一条 DSH 预发布线，都要把该元组补进范围**。
> 这也正是下面那条"尚未迁移"能诚实地写出来的原因——范围是可审计的，不是拍脑袋写的。

### 已知缺口：设置页面尚未迁移（下一步）

浏览器半仍在使用 0.1.7-rc.2 中**同样被移除**的 API：

| 用到的 | 0.1.7-rc.2 状态 |
|---|---|
| `settings.plugin.item` 槽位 | 已移除（0 处引用） |
| `settingsScope` 服务 | 已移除（0 处引用） |

因此**本版在 0.1.7-rc.2 上：模型可用、账号池照常轮换、定时任务与启动签到照常执行；
但设置卡片不显示**（客户端插件因硬依赖缺失而静默不启动，不会报红）。

仍可用的命令行入口：`dsh-workbuddy2api <doctor|status|pool|logout>`。

设置页迁移（`settings.section` + 去掉 `settingsScope`）在下一版完成。

## [0.5.0] - 2026-09-22

### 新增：每次启动 DSH 自动为全部账号签到

启动时（插件加载完成后）自动对所有账号执行一次签到，**不做定时排程** ——
就是启动那一次。

签到本身奖励不多，真正值得做的是它的**副作用**：一个因为积分耗尽而被打入
硬冷却的账号，会由它自己的签到恢复余额，于是池子可以立刻解冻它，而不是把
一个已经能用的账号闲置到次日 04:00。

对齐参考实现（workbuddy2api 的 `RunCheckinNow`）的五条语义：

| 语义 | 为什么 |
|---|---|
| 先查状态，**已签到就跳过** | 幂等，不重复打上游 |
| 已签到时**不当失败** | 否则第一次之后每次启动都显示"失败"，一个正常功能会被当成坏的 |
| 但**网络错误必须是失败** | 判据是"网关回了业务信封"（`workbuddy upstream ` 前缀）——连接中断也抛异常，绝不能算作已签到 |
| 签到后**再查余额并解冻** | 余额回来才解冻；签到证明计费通道正常，**不证明对话通道**，所以**不碰熔断** |
| 国际版**不发任何上游调用** | 国际版没有签到体系，请求只会 404 并污染日志 |

两处按本插件情况做的调整：

- **只清冷却域，不清熔断**：签到成功说明余额恢复、billing 通道健康，与触发熔断的
  chat 通道无关；熔断到期或下次 chat 成功才恢复。
- **禁用账号只记录余额、不解除禁用**：禁用是用户自己的开关，启动步骤不该
  悄悄把它打开。

### 修复：解冻时用错了账号 id（本次开发中发现）

签到后的解冻调用最初传的是 `credential.uid`，而**账号池是按另一个 id 建索引的**
（由 `uin` 派生的 `workbuddyAccountId`）。两者是不同的字符串，于是查找静默落空：
签到显示成功、结果也"看起来正常"，**但积分从未写回池子**。

已改为由调用方显式传入池的账号 id，并加了两条测试锁死：测试里的账号 id 故意
与 `credential.uid` **取不同的值**，一旦有人改回用 uid，测试立刻失败。

> 这个 bug 是实测发现的：连续两次真实启动后，`pool-state.json` 里
> 有账号记录却**始终没有 `credits` 字段**。修复后同样的启动流程写入了
> `credits: 4494, creditsCapacity: 4768`。

### 测试

新增 `tests/startup-checkin.spec.ts`（12 条）与池解冻用例（7 条），共 **226 条**。
覆盖：幂等成功 vs 网络失败、熔断不被清除、禁用账号不被启用、余额为 0 不解冻、
空余额不谎报解冻、国际版零调用、单账号失败不影响其他账号。

## [Unreleased]

### 发布

- **发布到 npm**：新增 `.github/workflows/publish.yml`，推 `v*` 标签即通过 npm
  trusted publishing（OIDC）自动发布，仓库里不存任何令牌，provenance 自动附加。
  首个版本需手动首发 —— trusted publisher 的配置入口在包的设置页，包不在 registry
  上就没法配。

### 修复

- **测试里的定时炸弹**：`tests/upstream.spec.ts` 的积分用例把赠包到期日写死成
  `2026-09-20`。`fetchCredits` 会丢弃已过期的一次性赠包，所以这个字面量在写下
  那一刻是对的、过期那天起就必然失败 —— **代码一行没改**。已改为相对当前时间推算，
  这样断言测的是聚合逻辑，不是跑测试的日期。

## [0.4.9] - 2026-09-18

对齐参考实现（workbuddy2api）的池策略，并回答三个分析项。完整对比见
[docs/pool-strategy-comparison.md](docs/pool-strategy-comparison.md)。

### 新增：模型级冷却（6004）

上游用 `code 6004` + 「将在 … 重置」表达「**这个模型的**用量超限」，
账号本身健康、其他模型照常可用。此前本插件把所有 429 都当账号级限流处理，
结果是**一个能用的凭据被整账号停掉数小时**，而且真正的病因完全看不见。

现在模型级限额只针对**那个模型**：账户级状态保持 `ready`，
`pick` 传模型名时才对它避让，换模型立刻可用。

### 新增：分层选号（成本分层）

按**该模型的实测扣费**（上游末帧 `usage.credit` ÷ `total_tokens`）把候选分层：

| 层 | 含义 | 处理 |
|---|---|---|
| 0 | 已实测免费 | **硬过滤**，最强偏好 |
| 1 | 无观测（含观测过期） | 次优 |
| 2 | 已实测收费 | 层内按单价升序排序 |

**「无观测」必须排在「已实测收费」之前**：新号的限免状态只能靠实测发现，
若已知收费的号恒压过未知号，那台免费的号就**永远轮不到、也永远学不到**。
观测带 **6 小时 TTL**，因此「夜间免费」这类时段优惠不会跨时段生效。

> 实测数据源确认可行：`deepseek-v4.1-flash` 上游回报 `credit = 0`，
> `glm-5.3` 回报 `credit = 0.17` —— 与目录标称的 x0.00 / x0.79 量级一致。

### 新增：WAF / Retry-After / 重置墙钟

- **WAF 403**（HTTP 403 且 body **无业务信封**）：APISIX 拦截页/空体/纯文本。
  此前落 `client`（只换号不罚）→ 每个请求换号、每个都撞墙、永不自愈。
  现在按账号级**软冷却**处理（**不禁用**：这是 IP/指纹维频控，到期自愈）。
- **Retry-After**：解析 `Retry-After`（秒）/ `retry-after-ms`（毫秒）/
  `x-ratelimit-reset`（epoch，推算剩余量）三种头，带 sanity 上限；
  非数字形态（如 HTTP-Date）宁缺毋滥，回落本地退避。
- **重置墙钟**：body 带「将在 … 重置」时**冷却到那个时刻**（按 UTC+8 解析，
  与宿主时区无关），**绝不指数堆加** —— 上游说了何时恢复，
  等更久只是白白闲置一个健康账号。优先级：body 墙钟 > Retry-After 头 > 本地退避。

### 顺带对齐的两处

- **session_dead 连续 3 次才判死**：单次 12153 多为抖动（闪断、刷新竞态），
  一次即禁用会误杀健康号。前两次软冷却，第 3 次才硬冷却；任意成功清零。
- **11102「该后端无此模型」负缓存**：`(账号, 模型)` 维度指数退避
  （6h 起、翻倍、封顶 24h），该模型实测又通了即清除。

### 修复

- **快过期窗口此前是个「能配但不生效」的死配置**：`policy.expiringSoonMs`
  默认 168h（7 天）且卡片里可改，但 `fetchCredits` 里**硬编码 3 天**，
  两者从未连接。于是 3–7 天内到期的积分**不会被标成快过期**，
  选号时也就不会优先消耗它们 —— 积分到期作废。
  现在窗口作为参数传入，两处调用点都取 `pool.currentPolicy().expiringSoonMs`，
  **卡片显示的数字与选号用的数字是同一个值**。
- **粘性重新分配改为「空闲优先」**：绑定失效后重新选号此前直接走加权随机，
  而权重偏向积分多的号，于是**多个新会话同时开启时容易一起落到同一个号**。
  现在优先选「未被任何会话绑定」的账号，一个空闲号都没有时才回退全候选集。
  仍持有有效绑定的会话在快路径就返回，**永远不会被挪动**。

### 验证

全部对**真实上游**实测，详见对比文档的实测表：

| 项 | 结果 |
|---|---|
| 成本账本的输入 | `deepseek-v4.1-flash` → `credit 0`；`glm-5.3` → `credit 0.17` |
| 11102 分类 | 真实发一个不存在的模型 → `kind = model_blocked` |
| 模型级冷却 | 真实 11102 后：账号 `ready`、该模型被拒、**其他模型仍可用** |
| 粘性键稳定性 | 同一会话两个 step（含 DSH 注入的时间戳消息）算出的键**完全相同** |
| 空闲优先分配 | 权重全给 a 时，三个新会话仍分到**三个不同账号** |

6004 与 WAF 403 依赖上游真实触发，无法在正常调用中人造，因此走
**真实函数级别的离线断言**（`tests/classify.spec.ts` 21 条）而非伪造上游错误。

## [0.4.8] - 2026-09-18

### 修复

- **重启/更新后各账号的请求统计与积分不再清零。** 先说清事实：这些数字此前
  **从来没有被持久化过** —— 它们只活在进程内存里，所以**任何一次重启**都会归零，
  不只是更新。你看到的现象是真的，原因不是「更新把它重置了」。

  现在这些**运行时事实**落到宿主自己的文件里：
  `$DSH_HOME/.workbuddy2api-pool-state.json`，逐区域、逐账号保存

  - 累计**成功 / 失败**次数
  - **最近使用 / 最近成功 / 最近出错**时间与**最近错误文本**
  - **缓存积分**（剩余、即将过期、总额度、读取时间）
  - **升级计数**（软冷却连击、熔断连败与次数、降权连败）—— 让退避在重启后
    接着算，而不是从「第一次」重新开始
  - **LRU 序号** —— 否则重启后所有账号都变成「一样新」，最近最少使用的偏好失效

  **刻意不保存 `inFlight`**：重启后没有任何请求在途，恢复一个「永远不会被
  释放」的在途计数会**永久占用该账号的并发额度**。

  **为什么放在自己的文件里，而不是 DSH 设置里**：这些是宿主每次请求都会更新的
  运行时事实，而 settings 是**卡片**在修订校验下写入的用户配置。两个写者共用一个
  文档必然互相覆盖 —— 卡片保存一次就会把宿主刚记下的计数抹掉。分开之后，
  设置文档仍然只装偏好（启用 / 权重 / 优先级），计数文件归宿主独占。

- `logout` 命令现在**一并清除计数字**：用户刚要求忘掉凭据，把统计留给一个
  可能被复用的账号 id 只会张冠李戴。

- `doctor` 新增一行计数状态与逐账号统计，并且**如实区分「没有计数文件」与
  「有文件但为空」**（此前会把「读不到文件」报成「已保存 0 个账号」）。

### 验证

在隔离环境里对**真实重启**做了往返验证：写入可区分的计数 → 停进程 → 重启 →
从池里读回。

| 字段 | 重启前写入 | 重启后读回 |
|---|---|---|
| successes | 41 | **41** |
| failures | 7 | **7** |
| credits | 4321 | **4321** |
| lastError | seeded… | **已恢复** |
| inFlight | （不持久化） | **0**（正确：没有在途请求） |

## [0.4.7] - 2026-09-18

### 修复

- **修改「自动执行」设置报 `client api: settings/mutate rejected "ops"`。**
  这是一处**两半互为因果**的 bug，截图里那两个**空白时间框**就是线索：

  1. 宿主的状态文档只带 `dailyAt`（一个 `"00:05"` 字符串），**没有数值的
     `hour`/`minute`**；卡片渲染的正是这两个数值字段，于是时间框是空的。
  2. 卡片随后把这份缺字段的对象**展开**进设置写入 —— 而写入前要过一道
     **strict codec**，它要求 `value.hour` 是真正的 number，`undefined`
     直接被拒，整次保存失败、什么都没写进去。

  两处都修了：宿主现在把 `hour`/`minute` 作为**数值**下发（`dailyAt` 退回纯展示用），
  卡片在写入前对每个字段做**归一化**（数值裁剪到 0–23 / 0–59、布尔强制成
  `true|false`），因此 `undefined` 在结构上不可能进入 payload。
  另外，携带旧形状（只有 `dailyAt`）的文档会被当作「无排程信息」而不是渲染成空框。

- **清空时间框不再悄悄变成 0 点。** `Number("")` 是 `0`，所以把小时框清空再
  触发一次 change 就等于把执行时刻改成 00:00。现在空输入被忽略，保留上一次的值。

### 验证方式（针对真实校验规则，不只是单测）

把 `settings/mutate` 的 `ops` 参数**真实生成的 schema** 取出来直接喂两种 payload：

| payload | 结果 |
|---|---|
| `hour/minute` 为 `undefined`（旧卡片的形状） | **REJECTED**（就是你看到的那个报错） |
| `hour: 0, minute: 5`（修好后的形状） | **ACCEPTED** |
| `hour: NaN`（清空输入框） | **REJECTED**（所以空白框也要挡掉） |

同时确认了这道拒绝发生在 **client 侧的 parseInput**，也就是**在
JSON 序列化之前** —— 所以「JSON.stringify 会把 undefined 丢掉」并不能救这次写入，
必须从源头保证字段有值。并在真实网关上跑通一次完整往返：
写入 → 读回 `00:05 / hour 0 / minute 5`。

## [0.4.6] - 2026-09-18

### 新增

- **版本 tab 与「池内账号」之间多了一行本池总额度**，也配一个环：
  `◕ 本池剩余 4 371 / 4 468 积分 · 98% · 共 1 个账号`。
  环的颜色与账号行里那个一致（≥50% 绿、≥20% 橙、更低红），
  尺寸略大一号以示这是**整池**而不是某一行。这是切 tab 之后第一眼要看的东西，
  不必再去逐行读每个账号。

  两条与账号行同源的诚实规则：

  - **没查过就说没查过**：积分是按需缓存的，没查过的账号**不会被当成 0**，
    而是单独报出来（`另有 N 个账号未查询`）；一个都没查过时整行显示
    「本池积分尚未查询 —— 点「刷新积分」」，而不是编一个 0；
  - **算不出比值就不画比值**：总额度覆盖不了总积分（目录没描述过的充值）时，
    环画灰、只报剩余数字，不标一个假的百分比。

### 修复

- 写这条汇总时发现自己踩了一个坑并已修正：宿主对**没查过积分的账号是不下发
  credit 条目的**，所以「遍历 credit 列表」会把这类账号**静默当成 0**。
  改成**遍历账号**再逐个查积分。新增 `tests/credits-total.spec.ts`
  6 条用例钉住这条（其中一条专门断言「没条目 = 未知，不是 0」）。

## [0.4.5] - 2026-09-18

### 修复

- **折叠把「自动执行」设置也收进去了。** 上一版我把整个任务区都塞进折叠体，
  于是「每天执行 / 执行时刻 / 启动后也执行一次」和「下次・上次执行时间」
  一起被收了起来 —— 可这些恰恰是**折叠状态下最想看一眼**的东西，
  也是我自己在折叠标题里承诺「展开可看…与自动执行设置」的矛盾之处。

  现在**自动执行设置留在折叠体之外**，永远可见，并**有了自己的标题**；
  折叠只影响那份长清单。折叠标题里的那句提示也改成了只描述清单
  （`已折叠 —— 展开可看每个任务及其进度`），不再指向设置。

- 顺带修正一处随之而来的数据流问题：折叠标题要显示汇总
  （`已完成 64/72`）和自动执行状态，而这两样都在任务文档里 ——
  但上一版为了省调用把任务文档改成「展开才拉」，于是刚打开时标题什么也没有。
  现在改为**每个区域每次页面加载只拉一次**（无论是否展开），
  仍然不随 60 秒轮询、仍然不在折叠时反复请求。

- `tests/card-markup.spec.ts` 增加一条结构断言：自动执行块**必须在折叠体之外**
  （按 JSX 的 div 深度判定）。已验证：把它挪回折叠体内，该测试会失败并说明
  「折叠清单会把自动执行的开关、时刻与上次/下次执行状态一起藏起来」。

## [0.4.4] - 2026-09-18

### 修复

- **任务列表展开之后收不回去。** 折叠开关本身是好的（标题行确实翻了状态），
  但列表**一直显示**：根因是 CSS 优先级 —— `hidden` 属性只是浏览器的
  内置样式（`[hidden]{display:none}`），而作者样式里给同一个元素写了
  `display:flex`，**作者样式永远赢过内置样式**，于是元素照样可见。
  截图里「已折叠」的标题配着仍展开的清单，正是这个组合。

  修法是补一条**优先级更高**的配对规则（`.dsm-wb2api-tasks-body[hidden]`
  是 0,2,0，压过裸类名的 0,1,0），所以它与书写顺序无关，将来样式表被重排
  或被宿主注入也不会失效。同一个卡片里另一处 `hidden`（外层卡片体）没有
  写 `display`，因此一直正常 —— 这次也一并核查过了。

- 新增 `tests/card-markup.spec.ts`：这类 bug **类型系统完全看不见**
  （状态对、DOM 属性对，只有像素不对），所以用一个测试守住 ——
  凡是「作者样式写了 `display` 又被 `hidden` 控制的元素」，
  都必须同时带 `[hidden]` 配对规则。已验证：把配对规则删掉，
  该测试会失败并直接指出是哪个类名。

## [0.4.3] - 2026-09-18

### 变更

- **任务列表加了一个总的折叠开关，默认折叠。** 一个账号就有 18 个任务，
  多账号时这一节会把下面的「池策略」和「模型」整个挤出屏幕。现在标题行
  本身就是开关（带箭头，点了展开/收起），折叠时标题行直接显示汇总：
  `成长任务 · 已完成 16/18 · 1 个可领取`，右侧的「刷新任务 / 一键完成」
  两个按钮**始终可见**，不用先展开。

- 折叠状态下**不再预取任务列表**。任务列表每个账号要打一次上游，
  折叠时不请求、展开时才拉（每个 tab 每次页面加载只拉一次），
  刷新按钮和「做任务」按钮仍然会按需拉取。

## [0.4.2] - 2026-09-18

### 修复

- **补上 7 个本可自动化、却被我错误排除的任务**，动作表现在与参考实现的
  **17 个**完全对齐（原 0.4.0 只有 10 个）。0.4.0 里我把这些标成「需官方客户端
  操作」，那个判断是错的 —— 它们同样只需要 API，所谓「客户端交互」只是需要一次
  **真实对话**，而真实对话本插件自己就能发：

  | 任务 | 实际做法（全部 API） |
  |---|---|
  | `first_buddy` | 活跃上报（解锁前置）→ `buddy/agreement` → `buddy/first` |
  | `Model_chat_GLM5.2` | 真实 glm-5.2 对话 → 对齐该模型的上报 |
  | `expert_5` | 专家市场**真实列表** → 召唤链 → 带 `X-Expert-Id` 真实对话 → `expert_actual_use` |
  | `Expert_team_use_3` | 同上，`expert_type=team`，3 个 |
  | `skill_1` | 真实对话 + `skill_info` 事件（`finishReason: tool_calls`） |
  | `Expert_lighthouse` | 轻量云专家召唤+使用链（`mode: LOCAL`、`type` 空、`cost: 0`） |
  | `black_cat` | 夜间窗口内 glm-5.2 对话 + 上报；**窗口外如实跳过** |

  关键实现：这些事件的 `requestId` 必须是**服务端返回的那个**（`cmb-` + 32 hex），
  自造 UUID 会被 `/v2/report` 收下然后**永远不计分**。因此新增的桌面形态对话会
  边流边扫 SSE，取到服务端 id 就放弃剩余正文（有界，不会被长回答拖住）。

- **不再把「什么都没做」记成「完成」**。动作现在可以回答「跳过 + 原因」：
  `black_cat` 在 23:00–08:00 窗口外不再报 done（那会是一条假记录），
  而是 `skipped` 并说明窗口限制。

### 说明

- 现在只有两个任务**确实不做**，且原因不是「技术上做不到」：
  `Expert_Philanthropy`（需要**真实捐款**，涉及真实支付）与
  `share_invite`（需要把邀请链接**分享给他人**，插件不代替用户打扰别人）。
  卡片会把这两条列出来并标注原因，其余全部可自动完成。

## [0.4.1] - 2026-09-18

### 新增

- **单个账号也能单独做任务。** 每个账号那一行多了「做任务」按钮，就在「签到」
  右边；它只跑这一个账号，不再需要滚到下面的任务区。
- **积分旁边的剩余额度环。** 积分数字左边多了一个 14px 的环，表示
  「当前积分 ÷ 已发放总额度」：50% 以上绿色、20% 以上橙色、更低红色，
  鼠标悬停显示百分比。额度来自上游的套餐清单 —— 月度套餐按本周期容量、
  一次性礼包按原始大小，所以这个比值就是「账号拿到的额度还剩多少」。
  **算不出来时（上游没给 size，或总额度小于剩余积分这类对不上的情况）
  只画灰环并说明「剩余额度未知」**，不会画一个假的百分比。

### 变更

- 「立即签到」按钮文案改为「签到」。

### 内部

- 积分答案新增 `capacity`（已发放总额度），随账号池缓存一起带上，
  因此卡片不需要为画环额外打上游：只有「刷新积分」才会真正请求套餐清单。

## [0.4.0] - 2026-09-18

### 新增

- **自动成长任务（每天 0 点 + 每次启动 DSH 各跑一轮）。** 卡片新增「成长任务」区：
  列出该账号在上游的全部任务（进度、奖励、可领取状态），并把**纯 API 就能点亮**的
  任务一键做完 —— 报名、上报行为事件、等上游异步计分、达标自动领奖，全自动。
  排程默认**每天 00:05** 跑一轮，并在 **DSH 启动约 20 秒后**再跑一轮，
  两处都可在卡片里改（开关 / 时刻 / 是否随启动跑）。

  为什么是「上报行为」而不是「点按钮」：上游只按**观察到的行为事件**计分。
  `tasks/accept` 只是报名，**不产生任何进度**；进度由服务端收到行为事件后
  异步点亮（实测上报后要数秒才刷新）。所以每个动作都做了三件事：
  ① 幂等跳过（已领取 / 已达标的任务直接跳过，不发任何请求）；
  ② 上报该任务考核的事件链；
  ③ **有界回读**（最多 4 次 × 3 秒）等异步计分落定，达标才调领奖接口。

  已实现 10 个纯 API 可点亮的动作：`chat_5`、`RichMeow_Chat`、`Buddy_App`、
  `Buddy_App_QQ`、`automation_1`、`Library_read`、`template_5`、
  `playbook_prompt`、`create_canvas`、`Hp_Appearance`。
  需要官方客户端内交互的任务（`expert_5`、`skill_1`、`first_buddy`、
  `Model_chat_GLM5.2`、`black_cat` 等）**在卡片里如实标注「需客户端操作」并说明原因，
  不会去发一条注定不计分的请求**。

- 领奖走 **web 域** `{webBase}/activity/growth/tasks/<code>/claim`（任务码在路径里、
  带 `x-client-platform: web`）。CLI 域的
  `/v2/activity/growth/tasks/reward/claim` **不存在**，一直返回
  "task not completed" —— 这是参考实现早期领奖全部失败的真实原因。

### 说明

- **区域门控**：国际版网关没有 growth 任务体系，其账号**不会**参与自动执行
  （卡片直接说明，定时轮次也把它列为「跳过：国际版没有任务体系」），
  而不是让它去撞 404。
- 一轮里账号是**串行**的，账号之间 3 秒间隔、单次上报之间 1.05 秒间隔
  （对齐参考实现实测的风控节奏）；同一时刻只可能有一轮在跑，
  重复触发会**加入**正在跑的那一轮而不是并发再来一遍。
- 卡片打开对应 tab 时才拉任务列表（每账号一次上游调用），**不随 60 秒轮询**。

## [0.3.1] - 2026-09-18

### 修复

- **登录成功之后仍然显示「unknown or expired sign-in session」。**
  上游的设备授权 state 是**一次性**的：兑换成功后它就被消费掉。
  而卡片是按 3 秒定时器轮询的，所以「完成」这个瞬间必然有多个轮询请求在飞 ——
  抢到的那一个兑换了 state 并成功落盘账号，其余几个随后被上游拒绝、
  在宿主侧被归类为「未知会话」，于是用户看到一个 404 报错，
  但账号其实已经加好了。现在：

  - 宿主对同一个 state **只放一个轮询出去**（单飞），并发的其余请求本地回
    `waiting`，不再重复兑换；
  - 登录完成后会话保留 **5 分钟**，期间重复轮询继续回答**同一个账号**，
    因此抢输的请求、以及页面刷新后的轮询，看到的都是成功；
  - 卡片侧同时按 state 丢弃过期应答（登录被取消或换了一个之后，
    旧请求的答案不再能改写界面）。

- 会话失效（state 过期 / 宿主重启）后，卡片原来仍显示「重新打开登录页」——
  那个链接指向的 state 已经作废，点开只会白跑一趟。现在这种**终态**直接换成
  「网页登录添加账号」，让用户重新发起。

## [0.3.0] - 2026-09-18

### 新增

- **网页登录添加账号。** 设置卡片里每个 tab 多了一个「网页登录添加账号」按钮：
  点它 → 浏览器打开 WorkBuddy 官方登录页 → 完成登录 → 账号自动落进**当前 tab
  那一侧**的账号池，免重启、免桌面端。实现走的是官方设备授权流程
  （`POST /v2/plugin/auth/state?platform=CLI` 取 `state`+`authUrl`，
  卡片每 3 秒轮询 `GET /v2/plugin/auth/token?state=`，
  完成时再 `GET /v2/plugin/login/account?state=` 带 Bearer 取 uid/nickname），
  与 workbuddy2api 的面板登录同源。
- 登录完成后顺带做三件事：把新账号拉进该区域的模型目录（否则要等下次重启才会
  出现在模型选择器里）、查一次余额、补一次当日签到；这三件失败只作为提示返回，
  不影响登录本身。

### 说明

- 凭据写入插件自有的 `$DSH_HOME/.workbuddy2api-auth.<accountId>.json`，
  **不写 WorkBuddy 桌面端的 auth 文件**；`source` 记为 `dsh`。
- **区域绑定**：登录会话记住发起它的 tab，浏览器流程在另一个 realm 完成时
  会直接报错而不是把账号塞进错误的池。
- **身份合并**：登录接口只返回 `uid`/`nickname`，不返回桌面端 auth 文件里的
  计费身份 `uin`。若同一账号已被本地发现，登录结果会**继承**已有的 `uin`，
  因此两条路径发现的同一个人只占**一条**池记录。
- 未完成的登录态记在浏览器 localStorage，页面刷新后卡片会继续轮询；
  15 分钟未完成自动失效，卡片给出可重新发起的提示。
- 卡片轮询在登录完成后照常刷新，新账号与它的积分会立刻出现在账号列表里。

## [0.2.0] - 2026-09-18

### 变更（破坏性）

- **拆成两个 provider、两个账号池、两份模型目录。** 0.1.x 把国内版与国际版
  账号并入一个池、目录取并集，结果撞上一个真实的数据问题：上游对同一个
  model id 在两个区域给出**不同语义** —— `deepseek-v4.1-flash` 在国际版是
  x0.00 的免费促销模型，在国内版是 x0.03 的收费模型。合并目录时
  「按 id 去重、先写者胜」让国内版的 x0.03 覆盖了国际版的 x0.00，
  于是只看到一个 ds4.1flash；更糟的是**显示的倍率与实际扣费可能不一致**，
  因为请求按 id 路由，落到哪个账号取决于池子的选号结果。

  现在：

  | | 国内版 | 国际版 |
  |---|---|---|
  | provider | `workbuddy2api` | `workbuddy2api-global` |
  | 账号池 | 独立 | 独立 |
  | 模型目录 | 独立（只列 CN 名单） | 独立（只列国际版名单） |
  | 设置 slot | `regions.cn` | `regions.global` |

  国际版账号现在以 `workbuddy2api-global` 出现在模型选择器里，
  `deepseek-v4.1-flash` 在自己的 provider 下正确显示为 x0.00，
  `deepseek-v4.1-flash-sg` 为 x0.03。

### 修复

- 凭据库按区域过滤：每个账号池只看得见自己区域的账号。
- `fetchModelsForCredentials` 拒绝跨区域合并（调用方传错区域会大声失败，
  而不是悄悄产生一份混合目录）。
- 设置卡片改为「国内版 / 国际版」两个 tab，账号池、策略、模型选择、
  未保存草稿完全按区域隔离；切 tab 不会丢另一侧的选择。
- 卡片新增账号启用开关的回显（`accounts[].enabled`），
  池里被停用的账号在列表里可见。
- CLI `doctor` / `status` / `pool` 按区域分别报告。

### 迁移

- 0.1.x 的扁平配置（`lastCatalog` / `enabledModelIds` / `poolState` /
  `pool` 在顶层）**不再读取**。旧目录是两区域并集、无法安全拆回单一区域，
  因此请在新卡片里对每个 tab 各按一次「从 WorkBuddy 刷新」并保存 ——
  这样得到的目录才是该区域真实的名单。（旧的顶层键保留在设置文件里
  不会被自动删除，你可以手动清理。）

## [0.1.1] - 2026-09-18

### 修复

- **从 git 安装不再需要 allowBuilds。** 0.1.0 把 `lib/` 排除在仓库之外，
  而 pnpm 对 git 依赖的判定是「有 prepack 且仓库里没有 main 文件 → 需要跑构建
  脚本」，于是每个用 `dsh plugin add git+https://…` 的人都会撞上
  `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED`。现在把构建产物 `lib/` 提交进仓库，
  pnpm 判定「不需要构建」，开箱即装。`prepack` 保留，`npm pack` / `npm publish`
  照旧先构建。
- 发布工作流新增一道闸：构建后断言 `git diff --exit-code lib/`，
  防止提交的构建产物与 `src` 脱节（那会让用户装到旧代码）。该断言只比内容、
  不比文件模式——`tsdown` 在 Linux 上会给 `lib/bin.js` 补可执行位，
  而 Windows 检出时没有这个位，一次 mode-only 差异会变成假警报。
  同时把 `lib/bin.js` 在索引里的模式记成 `100755`，与构建产物一致。

## [0.1.0] - 2026-09-18

首个版本：把 workbuddy2api 的多账号调度带进 DeepSeek Harness。

### 新增

- **账号池**（`src/pool.ts`）：四维健康状态（禁用 / 冷却 / 熔断 / 降权）、
  加权随机选号（余额 ×10、快过期积分 ×8、闲置补偿 0.5/h 封顶 5）、
  Top5 短名单 + 防撞号 + LRU 兜底、按账号的并发上限。
- **会话粘性**：同一会话固定同一账号，TTL 30 分钟滚动续期、GC 5 分钟。
- **失败迁移**：额度不足 → 次日 04:00 硬冷却；限流 → 600s 基数指数退避
  封顶 2h（已在冷却中不翻倍）；连续 3 次 5xx 熔断（30m 起翻倍封顶 6h）；
  无分类失败连败 5 次降权 10m、封顶 2h。
- **换号重试**：额度不足 / 限流 / 会话失效 / 5xx 自动换一个账号重试。
- **多账号凭据**：扫描桌面端 auth 目录，按 `uin` 去重；每个账号一份独立的
  刷新副本，N 个账号同时在线互不覆盖。
- **设置卡片**：账号列表（健康徽标、权重、启用开关、恢复、积分、签到）、
  池策略表单、模型管理。
- **同源路由**与 **CLI**（`doctor` / `status` / `pool` / `logout`）。
- **隔离测试环境**：`testenv/setup.mjs` + `testenv/run.cmd`。
